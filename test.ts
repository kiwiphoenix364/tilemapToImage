// tests go here; this will not be compiled when this package is used as an extension.

let image1 = tilemapToImg.generateImage(tilemap`level1`)
scene.setBackgroundImage(image1)
scene.setBackgroundColor(1)